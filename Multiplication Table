/*Multiplication table for a given number*/
#include<stdio.h>
#include<conio.h>
void main()
{
  int i,n,p;
  clrscr();
  printf("Enter a integer number:");
  scanf("%d",&n);
  for(i=1;i<=20;i++)
  {
     p=n*i;
     printf("%d*%d=%d\n",n,i,p);
  }
  getch();
}
