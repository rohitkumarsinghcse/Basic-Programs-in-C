/*Reverse of a number */
#include<stdio.h>
int main()
{
	int n,r,rev=0;
	printf("Enter number to be reversed:");
	scanf("%d",&n);
	while(n!=0)
	{
		r=n%10;
		n=n/10;
		rev=rev*10+r;
	}
	printf("Reversed number :%d",rev);
	return 0;
}
